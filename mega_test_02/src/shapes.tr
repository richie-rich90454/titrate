// Shape classes for the mega test.

public class Circle {
    private double radius;

    fn init(double radius) {
        this.radius = radius;
    }

    public fn area(): double {
        return 3.14159 * this.radius * this.radius;
    }

    public fn describe(): string {
        return "Circle(r=" + Double.toString(this.radius) + ")";
    }
}

public class Rectangle {
    private double width;
    private double height;

    fn init(double width, double height) {
        this.width = width;
        this.height = height;
    }

    public fn area(): double {
        return this.width * this.height;
    }

    public fn describe(): string {
        return "Rectangle(" + Double.toString(this.width) + "x" + Double.toString(this.height) + ")";
    }
}
