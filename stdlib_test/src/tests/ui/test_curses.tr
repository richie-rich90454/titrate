// Test for Curses — basic module access only
import tt::ui::Curses;

public fn test_curses_module(): void {
    io::println("PASS: Curses module loaded");
}

public fn run_all_curses_tests(): void {
    io::println("=== Curses.tr Tests ===");
    test_curses_module();
}
