// Test for Profile (tooling) — basic module access only
import tt::tooling::Profile;

public fn test_tooling_profile_module(): void {
    io::println("PASS: tooling Profile module loaded");
}

public fn run_all_tooling_profile_tests(): void {
    io::println("=== tooling/Profile.tr Tests ===");
    test_tooling_profile_module();
}
