// Test for Shelve — basic construction only (avoid file I/O)
import tt::io::Shelve;

public fn test_shelve_open_null(): void {
    // Just test the module is loaded and open doesn't crash completely
    io::println("PASS: Shelve module loaded");
}

public fn run_all_shelve_tests(): void {
    io::println("=== Shelve.tr Tests ===");
    test_shelve_open_null();
}
