{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Titrate Data File",
  "description": "Base schema for all Titrate stdlib data files. Requires _meta object with source, version, description.",
  "type": "object",
  "required": ["_meta"],
  "properties": {
    "_meta": {
      "type": "object",
      "required": ["source", "version", "description"],
      "properties": {
        "source": {
          "type": "string",
          "description": "The authoritative source of the data"
        },
        "version": {
          "type": "string",
          "description": "The version or edition of the source data"
        },
        "description": {
          "type": "string",
          "description": "A brief human-readable description of the data file"
        }
      },
      "additionalProperties": true
    }
  },
  "additionalProperties": true
}
