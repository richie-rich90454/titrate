[package]
name = "titrate_native"
version = "0.3.0-alpha"
edition = "2021"
description = "C-ABI native runtime bridge for the Titrate LLVM backend"

[lib]
name = "titrate_native"
path = "src/lib.rs"
crate-type = ["staticlib", "rlib"]

[dependencies]
trc = { path = "../trc" }
